# This tmux statusbar config was created by tmuxline.vim
# on Fri, 05 Mar 2021
set -g status-justify "left"
set -g status "on"
set -g status-left-style "none"
set -g message-command-style "fg=colour4,bg=colour0"
set -g status-right-style "none"
set -g pane-active-border-style "fg=colour4"
set -g status-style "none,bg=colour0"
set -g message-style "fg=colour4,bg=colour0"
set -g pane-border-style "fg=colour0"
set -g status-right-length "100"
set -g status-left-length "100"
setw -g window-status-activity-style "none"
setw -g window-status-separator ""
setw -g window-status-style "BOLD,fg=colour4,bg=colour0"
set -g status-left "#[fg=colour232,bg=colour4,BOLD] #S #[fg=colour4,bg=colour0,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=colour0,bg=colour0,nobold,nounderscore,noitalics]#[fg=colour4,bg=colour0] %Y-%m-%d  %H:%M #[fg=colour4,bg=colour0,nobold,nounderscore,noitalics]#[fg=colour232,bg=colour4] #h "
setw -g window-status-format "#[fg=colour4,bg=colour0,BOLD] #I #[fg=colour4,bg=colour0,BOLD] #W "
setw -g window-status-current-format "#[fg=colour0,bg=colour0,nobold,nounderscore,noitalics]#[fg=colour4,bg=colour0,BOLD] #I #[fg=colour4,bg=colour0,BOLD] #W #[fg=colour0,bg=colour0,nobold,nounderscore,noitalics]"
# END THEME

# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Start window numbering at 1
set -g base-index 1

# set keys when copying to vi keys
setw -g mode-keys vi

# pane switching with ctrl+a+arrow
bind -n C-h select-pane -L
bind -n C-l select-pane -R
bind -n C-k select-pane -U
bind -n C-j select-pane -D

# pane resize
bind-key -r h resize-pane -L
bind-key -r l resize-pane -R
bind-key -r k resize-pane -U
bind-key -r j resize-pane -D

# activity monitoring 
setw -g monitor-activity on
set -g visual-activity on

# enable automatic start with systemd
set -g @continuum-restore 'on'

# Tmux Plugin Manager
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
# list of configurations that should be acceptable for everyone
set -g @plugin 'tmux-plugins/tmux-sensible'
# enable tmux session saving
set -g @plugin 'tmux-plugins/tmux-resurrection'
# automatically save and load tmux sessions
set -g @plugin 'tmux-plugins/tmux-continuum'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'

